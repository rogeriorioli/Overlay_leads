import styled from 'styled-components';

export const Container = styled.div`
   button {
     background : transparent;
     color : #fff;
     cursor : pointer;
     position : absolute;
     top: -30px;
     right : 0;
     border : none;
   }   
`;
