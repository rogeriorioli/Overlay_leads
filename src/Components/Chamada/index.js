import React from 'react';

import { Container } from './styles';

const Chamada = ({hide}) => (
  
  <Container>
    <p className={`${hide}`}>Aproveite todas as vantagens de uma solução completa para seu recrutamento!</p>
  </Container>
  
 ) ;

export default Chamada;
